{
  "name": "ho-krat-app",
  "version": "1.0.0",
  "private": true,
  "description": "HO Krat Android wrapper",
  "scripts": {
    "android:prepare": "npx cap add android || true && npx cap sync android",
    "android:apk": "npm run android:prepare && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
