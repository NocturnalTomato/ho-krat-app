name: Probe Standings

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Probe standings
        run: |
          BASE="https://ho-krat-spond-trigger.lucdegoeij.workers.dev"
          curl -sf "$BASE/standings" | python3 -m json.tool
